If the user said /overnight --now, this is the noon dry-run of 5am. Run `bash bin/overnight.sh` from this business folder (`chmod +x bin/overnight.sh` first if needed). Wait for it to exit. Then show `reports/morning-brief-YYYY-MM-DD.md` and `logs/overnight-YYYY-MM-DD.log`. That wrapper is what launchd runs: grok --always-approve --cwd --max-turns 40 with allow/deny globs. Do not start the overnight-ops workflow in this session instead — an in-session run does not write the overnight log and does not prove 5am.

If they said /overnight without --now, start the overnight-ops workflow in this session. Pass today's date as YYYY-MM-DD (workflows cannot call timestamp()). agent_budget 8. Prefer name overnight-ops or script_path .grok/workflows/overnight-ops.rhai; if folder trust refuses the path, pass the script inline. Do not spawn listing or creative. Overnight may spawn max 2 flagged specialists (inventory, ads, customer). Logic may reorder those three, not raise the cap.
