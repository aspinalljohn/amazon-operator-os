If the user said /overnight --now, run the overnight-ops skill once (Grok Bot default). On Grok Build CLI advanced path only, you may run `bash bin/overnight.sh` from this business folder and wait for exit, then show `reports/morning-brief-YYYY-MM-DD.md`. Do not tell Grok Bot users to install Grok CLI.

If they said /overnight without --now, start the overnight-ops workflow only when the Grok Build workflow tool is available; otherwise run overnight-ops directly. Pass today's date as YYYY-MM-DD. agent_budget 8 when using workflow. Never spawn listing or creative. Cap 2 flagged specialists (inventory, ads, customer).
